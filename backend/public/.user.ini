; Allow restoring a full site backup ZIP through the admin UI.
; Nginx must also allow this size (see deploy/nginx.conf.example client_max_body_size).
upload_max_filesize = 256M
post_max_size = 256M
max_execution_time = 300
max_input_time = 300
