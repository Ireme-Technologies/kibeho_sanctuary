import styles from './CareersContent.module.css'

/**
 * PLACEHOLDER — replace this file's contents with the real section
 * (CareersContent.jsx / CareersContent.module.css / CareersContent.js) once it's designed.
 * The import path in the page file stays the same, so this is a
 * drop-in overwrite, not a rewire.
 */
export default function CareersContent() {
  return (
    <section className={styles.placeholder}>
      <div className="container">
        <p className={styles.label}>CareersContent</p>
      </div>
    </section>
  )
}
