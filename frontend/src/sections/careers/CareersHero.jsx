import styles from './CareersHero.module.css'

/**
 * PLACEHOLDER — replace this file's contents with the real section
 * (CareersHero.jsx / CareersHero.module.css / CareersHero.js) once it's designed.
 * The import path in the page file stays the same, so this is a
 * drop-in overwrite, not a rewire.
 */
export default function CareersHero() {
  return (
    <section className={styles.placeholder}>
      <div className="container">
        <p className={styles.label}>CareersHero</p>
      </div>
    </section>
  )
}
